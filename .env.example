# Backend Environment Variables
# Database Configuration (for production)
DB_URL=jdbc:postgresql://localhost:5432/grocerydb
DB_USERNAME=admin
DB_PASSWORD=secret

# JWT Configuration
JWT_SECRET=your-super-secret-key-that-is-long-and-secure-for-production-use
JWT_EXPIRATION_MS=86400000

# Note: For development, the application uses H2 in-memory database by default
# No environment variables are required for the frontend at this stage
